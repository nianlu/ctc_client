import React, { useState, useEffect } from 'react';

const Word = (props) => {

  return (
    <div>
      <span style={{fontSize: '10rem'}}>kāng dǐng gàng</span>
    </div>
  )
}

export default Word
